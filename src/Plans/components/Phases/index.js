import React from 'react';

/**
 * Renders layout of plan activities arranges phase wise
 *
 * @function
 * @name PlanPhases
 *
 * @returns {ReactNode}
 *
 * @version 0.1.0
 * @since 0.1.0
 */
const PlanPhases = () => <div>These are plan activities per phase</div>;

export default PlanPhases;
